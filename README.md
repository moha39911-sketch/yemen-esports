# Yemen Esports Federation website

Static GitHub Pages-ready build.

## Deploy
Upload `index.html` and the `assets` folder to the repository root, then enable GitHub Pages from the `main` branch root.

All site images use local relative paths so they work on GitHub Pages.
